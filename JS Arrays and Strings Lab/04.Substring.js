function substring(text, start, count) {
    console.log(text.substring(start, start + count));
}

// def substring(text, start, count):
//     print(text[start:start + count])

function substr(text, start, count) {
    console.log(text.substr(start, start + count))
}